<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whu.checky.mapper.TagMapper">
    <update id="incTagCount">
        UPDATE tag set tag_count = tag_count + 1 where tag_id = #{tagId}
    </update>
    <update id="incPassNum">
        UPDATE tag set pass_count = pass_count + 1 where tag_id = #{tagId}
    </update>
</mapper>